// Replace this with your actual local IP address when running on a physical device.
// You can find your IP by running `ipconfig getifaddr en0` on Mac or `ipconfig` on Windows.
export const API_BASE = 'http://10.10.70.134:6969';
